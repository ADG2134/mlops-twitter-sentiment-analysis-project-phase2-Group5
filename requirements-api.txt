# Runtime dependencies for the FastAPI service / Docker image.
# Deliberately excludes dvc/mlflow (training-only, keeps the image small).
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
scikit-learn>=1.4
pandas>=2.0
pyyaml>=6.0
